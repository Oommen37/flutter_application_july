import 'package:flutter/material.dart';

class Tour_signup extends StatelessWidget {
  const Tour_signup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
