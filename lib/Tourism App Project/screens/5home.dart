import 'package:flutter/material.dart';

class Tour_home extends StatelessWidget {
  const Tour_home({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}